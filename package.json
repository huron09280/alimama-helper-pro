{
  "name": "alimama-helper-pro",
  "version": "5.24.0",
  "private": true,
  "description": "阿里妈妈多合一助手 (Pro版) - Browser extension + userscript build pipeline",
  "type": "module",
  "scripts": {
    "check:syntax": "node build/check-syntax.mjs",
    "build": "node build/build.mjs",
    "package": "node build/package.mjs",
    "verify": "npm run check:syntax && npm run build && npm run package"
  },
  "devDependencies": {
    "crx3": "^2.0.0"
  }
}
